language: node_js

node_js:
  - 'lts/*'

install:
  - npm install

script:
  - npm run test

before_deploy:
  - npm run build

notifications:
  email:
    on_success: never
    on_failure: always

deploy:
  provider: pages
  skip_cleanup: true
  verbose: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
